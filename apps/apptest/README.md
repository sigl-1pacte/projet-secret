# apptest

Generated by the CNP provisioning API.

## Metadata

- Team: `maxence-team`
- Framework: `fastapi`

## Run locally

```sh
uv sync
uv run python -m app.main
```

## CNP contract

The original request is stored in `.cnp/app-request.json`.
If logging is enabled, application logs must be written to stdout/stderr so Grafana Alloy can forward Pod logs to Loki.
